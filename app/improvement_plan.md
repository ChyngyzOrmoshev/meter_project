cat > ./app/improvement_plan.md << 'EOF'
# План улучшений системы «ИнфоЭнерго»

> Версия: 1.0  
> Дата: 2026-06-16  
> Статус: в работе  

---

<!-- ## 🎯 Приоритет 1 (критически важно)

- [ ] **Статус синхронизаторов**  
  Создать коллекцию `sync_status` в MongoDB, куда каждый робот пишет время последнего успешного запуска, количество обработанных записей и ошибки. На главной странице выводить индикатор работы каждого робота.

- [ ] **Графики потребления**  
  В раздел «Сбор показаний» добавить возможность построения графика (по дням/неделям) для выбранного счётчика. Использовать `plotly` или `altair`. -->

<!-- - [ ] **Логирование в файл**  
  Заменить `print` на модуль `logging` с ротацией логов (например, `/var/log/infoenergo.log`). Настроить уровни логирования (INFO, ERROR).

--- -->

<!-- ## ⚡ Приоритет 2 (сильно упрощает жизнь)

- [ ] **Редактирование справочника моделей через веб-интерфейс**  
  Добавить CRUD (добавление/редактирование/удаление) моделей прямо в разделе «Справочник моделей». Сейчас только через Excel.

- [ ] **Экспорт данных в Excel/CSV**  
  В таблицах (Реестр устройств, Журнал показаний) добавить кнопку «Скачать» для выгрузки текущего фильтра.

- [ ] **Пагинация в Реестре устройств**  
  Если количество приборов станет большим (>500), ввести постраничный вывод (по 50–100 записей). -->

---

## 🛡️ Приоритет 3 (безопасность и надёжность)

<!-- - [ ] **Healthcheck для контейнеров**  
  Добавить в `docker-compose.yml` проверку работоспособности (например, через `curl` к `/healthz` эндпоинту или проверку ответа Streamlit). -->

<!-- - [ ] **Автоматический бэкап MongoDB**  
  Настроить cron-задачу на создание дампа базы раз в сутки (хранить на хосте, например, в `./backups`). -->

<!-- - [ ] **Логирование действий пользователей**  
  Записывать в отдельную коллекцию `audit_log`: кто, когда, какое действие совершил (вход, добавление прибора, удаление, изменение показаний).

--- -->

## 🖥️ Приоритет 4 (интерфейс и удобство)

<!-- - [ ] **Фильтр по дате добавления в Реестре устройств**  
  Добавить возможность отфильтровать приборы по диапазону дат создания. -->

- [ ] **Индикация загрузки**  
  Добавить `st.spinner()` или прогресс-бар при выполнении тяжёлых запросов.

- [ ] **Сортировка по столбцам**  
  В таблицах разрешить сортировку по колонкам (серийный номер, дата, значение).

---

## 🧪 Приоритет 5 (перспективные задачи)

- [ ] **Сводная панель (дашборд)**  
  На главной странице показывать: общее количество приборов, последние показания, статусы синхронизаторов.

- [ ] **Массовое редактирование устройств**  
  Возможность менять статус (активный/ремонт) для группы приборов.

- [ ] **Генерация PDF-отчётов**  
  Построение отчёта за выбранный период с графиками и таблицей показаний.

---

## 📌 Отметки о выполнении

| Задача | Статус | Дата начала | Дата завершения |
|--------|--------|-------------|-----------------|
| Статус синхронизаторов | ❌ не начато | — | — |
| Графики потребления | ❌ не начато | — | — |
| Логирование в файл | ❌ не начато | — | — |
| Редактирование моделей | ❌ не начато | — | — |
| Экспорт данных | ❌ не начато | — | — |
| Пагинация в Реестре | ❌ не начато | — | — |
| Healthcheck | ❌ не начато | — | — |
| Бэкап MongoDB | ❌ не начато | — | — |
| Логирование действий | ❌ не начато | — | — |
| Фильтр по дате | ❌ не начато | — | — |
| Индикация загрузки | ❌ не начато | — | — |
| Сортировка | ❌ не начато | — | — |
| Дашборд | ❌ не начато | — | — |
| Массовое редактирование | ❌ не начато | — | — |
| PDF-отчёты | ❌ не начато | — | — |

---

*План может корректироваться по мере развития проекта.*
EOF